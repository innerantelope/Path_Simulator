# Contributing

1. Create a branch from `main`.
2. Keep backend changes covered by GoogleTest.
3. Keep frontend changes passing `npm run build`.
4. Prefer small, focused pull requests.
5. Document new REST or WebSocket fields in `docs/API.md`.
